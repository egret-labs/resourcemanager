{
  "name": "res",
  "version": "3.9.0",
  "description": "",
  "scripts": {
    "publish": "node tsc_local/local/tsc.js && egret-cli libraries publish",
    "test":"egret-cli upgrade res-v4 test --x x && egret build test -e && egret run test"
  },
  "author": "wander",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^2.1.0-dev.20160814"
  }
}
